<%= simple_form_for [@cocktail, @dose] do |f| %>
   <%= f.association :ingredient, collection: @ingredients %>
   <%= f.input :description, label: "Amount" %>
   <%= f.button :submit,label: "Add_Ingredient" %>
<%end%>
